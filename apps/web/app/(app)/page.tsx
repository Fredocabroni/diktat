// Home feed placeholder. Real news_topic drops + battle cards land in
// later phases; Phase 2 only needs a navigable shell.

export default function HomePage() {
  return (
    <section className="mx-auto max-w-md px-4 py-6">
      <h1 className="font-display text-2xl font-bold">Home</h1>
      <p className="mt-2 text-text-secondary">Your feed will land here.</p>
    </section>
  );
}
