
- **Capture or infer user timezone at signup** — currently defaults all users to `'America/New_York'`; the streak engine's "local 9PM" and "local midnight" properties only become genuinely local once this lands. Couple with the web-push delivery PR since both touch the same per-user notification surface.
